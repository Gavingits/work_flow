Run Script Tool
===============

This tool takes a template script, performs text replacements based on its configuration, saves a new script, and then executes it in a new terminal window.

Inputs:
- script_template_file: The path to the template script.

Outputs:
- output_floader: The directory where the new script will be saved. The output socket will point to the new script file itself.
